# Env
.env
.env.api_keys
.env.*.local
.env.*

# Cookies
info/cookies.json

# Python
__pycache__/
*/__pycache__
*.pyc
.venv/
venv/
.pytest_cache/

# Outputs
screenshots/*
data/*
!data/.gitkeep
*.faiss

# Logs / harness
.remember/
init_plan.md

# MCP config holds the Mongo connection-string secret — never commit
.mcp.json

# Test outputs
test_artifacts/
results/
info/cookies.stale
data/event_logs/

/test_artifacts
/feature
/data
/info
# Upstream skill used only to capture the eval baseline; not a dependency.
last30days-skill/
.coverage

.playwright-mcp/
# Google OAuth client secret (live credential — never commit)
client_secret_*.json

# Mined experiment scratch — features already ported into lib/
headroom/
Agent-Reach/
